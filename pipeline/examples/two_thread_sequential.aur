// Test: two spawned threads with delay
// Expected: 10 (5+5)

shared counter: int = 0;

fn worker() {
  atomic.add(counter, 5);
}

fn main() -> int {
  let t1: thread = spawn worker();
  join t1;
  let t2: thread = spawn worker();
  join t2;
  return atomic.load(counter);
}
