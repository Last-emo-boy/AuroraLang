// Test float spill in loop with 8 variables
fn main() {
    let a: float = 1.0;
    let b: float = 2.0;
    let c: float = 3.0;
    let d: float = 4.0;
    let e: float = 5.0;
    let f: float = 6.0;
    let g: float = 7.0;
    let h: float = 0.0;  // Loop counter
    
    print_float(a);  // 1.0
    
    while h < 3.0 {
        a = a + 1.0;
        print_float(a);  // 2.0, 3.0, 4.0
        h = h + 1.0;
    }
    
    print_float(a);  // Should be 4.0
}
