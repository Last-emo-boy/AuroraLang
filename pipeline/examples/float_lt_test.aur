module test {
    fn main() -> int {
        let x: float = 5.5;
        let y: float = 3.5;
        let result: int = 10;
        
        // Test: y < x should be true (3.5 < 5.5)
        if y < x {
            result = result + 5;
        }
        
        // Expected: 10 + 5 = 15
        return result;
    }
}
