module test {
    fn main() -> int {
        let x: float = 5.5;
        let y: float = 3.5;
        let result: int = 0;
        
        // Single test: x > y should be true
        if x > y {
            result = 42;
        }
        
        return result;
    }
}
